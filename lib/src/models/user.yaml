class: User
table: user
fields:
  userInfo: module:auth:UserInfo?, relation(onDelete=Cascade)
  birthday: DateTime
  goals: List<Goal>?
indexes:
  user_info_id_unique_idx:
    fields: userInfoId
    unique: true